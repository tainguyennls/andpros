<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nguyenhuutai.studentapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.SlashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.HomeScreenActivity" />
        <activity
            android:name=".views.BottomNavigationActivity"
            android:label="@string/title_activity_bottom_navigation" />
        <activity android:name=".views.MainActivity" />
        <activity android:name=".views.DetailLecturerActivity" />
        <activity android:name=".views.GroupActivity" />
        <activity android:name=".views.DetailDoanHoiActivity" />
        <activity android:name=".views.DayHocActivity" />
        <activity android:name=".views.DetailDayHocActivity" />
        <activity android:name=".views.ViecLamActivity"></activity>
    </application>

</manifest>