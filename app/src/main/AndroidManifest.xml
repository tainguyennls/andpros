<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nguyenhuutai.studentapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.SlashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.HomeScreen" />
        <activity
            android:name=".view.BottomNavigationActivity"
            android:label="@string/title_activity_bottom_navigation" />
        <activity android:name=".view.MainActivity" />
        <activity android:name=".view.DetailLecturerActivity" />
        <activity android:name=".view.DoanHoiActivity" />
        <activity android:name=".view.DetailDoanHoiActivity" />
        <activity android:name=".view.DayHocActivity" />
        <activity android:name=".view.DetailDayHocActivity" />
        <activity android:name=".ViecLamActivity"></activity>
    </application>

</manifest>